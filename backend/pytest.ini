[pytest]
addopts = -m "not integration"
markers =
    slow: integration tests that wait for background runner ticks
    integration: tests that require network, a live server, broker, or external model
