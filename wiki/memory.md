# QuantG Auto-Memory Ledger

This file is a persistent memory ledger. AI agents (Claude, Codex, Gemini) are required to read this file at the start of a session and append their session summary and learnings here at the end of every session.

## Session Logs

| Date | Agent | Session Summary & Core Decisions | Key Technical Challenges & Solutions |
|---|---|---|---|
| 2026-06-18 | Codex | Stage 1A Event Bus Redesign & route extraction completed. | Extracted remaining API routes out of `server.py` to modularize code. |
| 2026-06-19 | Antigravity (Gemini 3.5 Flash) | Implemented the Wikis, Backlinks, and Auto-Memory Knowledge Hub system. Created folder structure, added routing rules to CLAUDE.md/AGENTS.md, created REST endpoints in routes/wiki.py, and built the Wiki.jsx page with physics-simulated SVG link graph. | Resolved Windows Temp directory access permission errors in backend tests by implementing tempfile.TemporaryDirectory with a local scratch path fallback. Resolved React Hooks warnings in visual graph SVG. |
| 2026-06-19 | Antigravity | Refactored the front-end codebase to modularize Dashboard, Strategies, OpsConsole, Wiki, and AIBot page templates into cleaner subcomponents. Wired a central ExecutionStateProvider to eliminate redundant polling loops. Added flashing green/red LTP/P&L animations on positions. | Ensured all imports matched path depths when extracting helper subcomponents, added a missing TypingIndicator component definition to AIBot, and successfully deployed to the VPS using docker-compose rebuilds. |
| 2026-06-19 | Antigravity (Gemini 3.5 Flash) | Implemented TASK-047 (Feed + token health watchdog/alert). Exposes connection time on `UpstoxMarketDataFeedV3` status payload. Adds `feed_stalled` and `feed_stalled_reason` logic to `get_user_upstox_status`. Integrates watchdog check into `_strategy_health_loop` to raise critical alerts. Updates `ReadinessBanner` to render stalled feed messages. | Added unit tests to `test_trading_readiness.py` to cover connection age calculations and mock validation caches to verify checks pass cleanly. |
| 2026-06-19 | Antigravity | Implemented TASK-048 (debit spread structure support in option_builder, spread_lifecycle, signal_manager, position_monitor, execution_state, and frontend components like Positions, StrategyCard, RuntimeSettingsForm, and AboutStrategyModal). Implemented TASK-049 & TASK-050 (declaration of research baseline marker and pausing of cumulative buyer losers Nifty Quick EMA, Upstox Nifty ATM Momentum, and Upstox Banknifty ATM Breakout). | Ensured sign conventions for debit spreads (position side LONG, net cost paid, exit TP/SL triggered on rise/fall of spread value) were properly handled. Handled gitignore of scratch files by force-adding declare_baseline.py to git. Deployed changes on the VPS and executed the database updates. |
| 2026-06-19 | Antigravity | Researched Nous Research's Hermes Agent architecture and devised an integration strategy for QuantG. | None (pure research and planning task). |
| 2026-06-19 | Codex | Reviewed current TASKS.md, CLAUDE.md roadmap, prior memory notes, and wiki session history to suggest the next highest-value QuantG product and architecture improvements. | Kept the pass planning-only; no app code or trading behavior changed. |
| 2026-06-19 | Codex | Researched current Hermes Agent docs/GitHub and mapped a QuantG-specific integration roadmap for read-only research, monitoring, memory, scheduled audits, and future approval-gated operations. | Preserved the existing QuantG safety boundary: Hermes should start as read-only and should not place/cancel/modify trades or mutate strategy/risk settings. |
| 2026-06-19 | Codex | Broke the proposed Hermes Agent integration into a staged QuantG task backlog covering sidecar setup, read-only tools, scheduled reports, in-app surface, skills, approval-gated ops, and long-term research workflows. | Kept the task design safety-first and identified founder inputs needed before implementation. |
| 2026-06-19 | Codex | Added [[Hermes Agent Integration Roadmap]] to the QuantG app memory under `wiki/Projects/`, capturing the full staged build plan and pending founder inputs. | Kept the roadmap read-only-first and linked it from the auto-memory ledger for future agent discovery. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Completed TASK-H005, TASK-H006, and TASK-H007 in routes/ai.py. Extended tool response envelope with source, stale, confidence, warnings, user, and account. Wired 12 Stage-2 read-only tools and aliases. Implemented async tool auditing to db.agent_tool_audit. Rebranded the Ask QuantG Agent interface as Hermes Analyst Co-Pilot (TASK-H011) and implemented cited tool output cards in frontend chat feed (TASK-H012). | Resolved keyword extraction and query search logic in search_wiki. Avoided circular import issues via inline imports. Added new backend test cases in test_agent_tools.py. Ensured proper serialization of tools_used dictionary structure to MongoDB to persist citation metadata across restarts. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Completed TASK-H003, TASK-H004, TASK-H008, TASK-H009, and TASK-H010. Created isolated Hermes Sidecar directory, Dockerfile, requirements, and main python agent daemon. Configured .env.hermes.example template and updated docker-compose.yml. Authored DEPLOY_HERMES.md runbook. Wired timezone-proof market hours, watchdog checker, pre-market readiness report, and EOD trading summary alert to Telegram. | Resolved directory relative import errors in mock test suite by referencing sibling directories from root correctly. Handled .env ignore overrides by modifying .gitignore. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Completed TASK-H023 and TASK-H025. Implemented the inbound Telegram command bridge in the Hermes sidecar (`hermes/agent.py`). Integrated offset-tracked long-polling, authorization check for `TELEGRAM_CHAT_ID`, and routing for `/status`, `/pnl`, and `/why` / free-text commands. Stripped `PROPOSED_ACTION` blocks to enforce the in-app approval gate. Updated deployment runbook and configuration template with new vars and supported commands. | Initialized offset using `offset=-1` to skip old messages on startup. Wrapped the polling/routing handlers in comprehensive try-catch blocks to prevent Telegram API errors from stopping the scheduled EOD and watchdog loops. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Completed Stage 5 (Skills & Wiki Sync) and Stage 6 (Strategy Research Assistant). Created `backend/core/skills.py` defining 8 operator playbooks, exposed `GET /agent/skills`, and injected playbooks into system prompts in `backend/routes/ai.py`. Dynamically synchronized disk-based Obsidian wiki notes to MongoDB prior to `search_wiki` tool runs. Parameterized `get_backtest_summary` tool with query param parsing. Created `wiki/Projects/Strategy Experiment Ledger.md`. Added weekly strategy scorecard ranking report (Fridays 09:00 IST) to Telegram in `hermes/agent.py`. | Fixed pytest hang in `test_run_agent_tool_search_wiki` by mocking/patching `sync_wiki_directory` to avoid hitting live database and disk operations during unit tests. Added unit test for `get_backtest_summary` to cover custom strategy and date range extraction logic. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Implemented Stage 7 Approval-Gated Operations. Extended `routes/ai.py` to allow proposing new non-trading action types (`draft_wiki_note`, `draft_task_entry`, `draft_incident_report`, `draft_pr_summary`). Added `GET /agent/actions/pending` route. Built visual Approvals Queue tab in AIBot page displaying pending actions as cards with Approve/Reject actions, and wired warning badge count indicators to Layout sidebar and mobile bot menu. | Safeguarded against malicious actions by introducing strict allowed_actions check, forbid keyword checks, and mock-open testing pattern. Mocked `builtins.open` to safely verify task appending to `TASKS.md` in tests without side-effects on development repositories. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Implemented TASK-H024 (Proactive behavioral alerts). Added configurations for `DROUGHT_CUTOFF_IST`, `DRAWDOWN_ALERT_FRAC`, and `LOSS_STREAK_N` inside `hermes/agent.py`. Built `run_behavior_watch()` watchdog checking for trade drought, daily drawdown breach, and consecutive losing trades. Integrated periodic execution in the watchdog loop. | Added automated tests to `test_hermes_sidecar.py` for each behavioral alert. Resolved mock datetime timezone-aware comparison and `fromisoformat` mock delegation pitfalls in unittest.mock by using `mock_dt.fromisoformat.side_effect = datetime.fromisoformat`. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Completed Stage 8 Incident Commander (TASK-H021 & TASK-H022). Implemented `get_core_events` and `get_agent_tool_audit` read-only tools in `backend/routes/ai.py`, registered them, and implemented their queries. Updated the system prompt in `backend/routes/ai.py` and the `quantg-incident-postmortem` playbook in `backend/core/skills.py` to compile chronological timeline details. Added unit tests for new tools and the `draft_incident_report` action executor. | Handled the date string generation dynamically in tests using `datetime.now(timezone.utc).strftime("%Y-%m-%d")` to avoid hardcoding date expectations and prevent test failures over time. |
| 2026-06-20 | Antigravity (Gemini 3.5 Flash) | Reviewed and audited the remaining task queue in `TASKS.md` for open tasks. Identified five open tasks under Priority 0 (Profitability Campaign). Produced a detailed technical implementation design for each task, saving it to `remaining_tasks_analysis.md`. | None (pure research and analysis task). |
| 2026-06-21 | Claude (Cowork) | Audited the §11.4 multi-writer claim on `strategy_positions`/`today_pnl` against the actual code (not just the design doc). Found `today_pnl` is already safe — `portfolio_ledger.py` and `spread_lifecycle.py` write disjoint trade-type domains and are each independently idempotent (`processed_fill_ids` index / atomic OPEN→EXITING claim) — so no fix was made there; corrected CLAUDE.md §11.6 to say so. Found and fixed a real race on `strategy_positions`: `position_monitor.py` (30s loop) and `position_guardian.py` (5s loop) each await an LTP quote before writing `unrealized_pnl`/`last_ltp`, with no status filter on the write — a concurrent close mid-await could restamp stale mark-to-market data onto an already-CLOSED position. Added `status: {"$in": [...]}` guards to the 4 affected `update_one` calls (2 in each file), mirroring the compare-and-swap pattern `spread_lifecycle.py` already used for its close claim. | Verified the fix by tracing every call site of `process_fill`, `close_credit_spread`/`close_debit_spread`, and `_close_strategy_positions` to confirm idempotency guarantees before touching code, rather than acting on the design doc's claim at face value. `position_reconciler.py` and `server.py` admin-route writes to `strategy_positions` remain unaudited — flagged as the next piece of this work. |
| 2026-06-21 | Antigravity (Gemini 1.5 Pro) | Completed the Equity Phase Campaign (TASK-EQ-01 to TASK-EQ-05). Enabled live WS feed caching for cash equities, subscribed equities on entry, implemented symmetric exit pricing with REST lookup fallback and pricing-failure raise, rejected nominal fills <= ₹0.05, enforced exit reason overrides based on P&L, calculated dynamic position sizing from capital allocation, and isolated manually paused strategies from bulk resume endpoints. | Fixed mock awaitable TypeErrors in test_equity_campaign.py by explicitly structuring mock database collections and attributes. Fixed AttributeError on option_ledger patch by patching server instead of routes.ops. Corrected a critical is_option regex bug in PaperAdapter that misclassified RELIANCE cash equity as an option. |
| 2026-06-21 | Antigravity (Gemini 3.5 Flash) | Overhauled the "Ask Agent" (Hermes Analyst Co-Pilot) UI/UX. Implemented a toggleable 3-column grid layout, expanded the grid to 100vh-130px to maximize vertical space, replaced the PageHeader card with a slim inline page header, and nested the suggestions pills into a collapsible, empty-state-dismissible bar that hides when typing. Made markdown tool citations collapsible. | Fixed viewport height constraints by removing the card PageHeader wrapper, reclaiming vertical space, and wrapping citations/suggestions with toggling states. Verified all changes compile cleanly using npm build. |
| 2026-06-21 | Antigravity (Gemini 3.5 Flash) | Completed Knowledge Hub & Second Brain page enhancements including: full-text search snippets, Editor wikilink autocomplete suggestion box, Unlinked Mentions discovery tabs with auto-link builders, Trading Rules configuration drift auditor warning alerts, and the AI Webpage auto-summarizer URL modal. | Resolved edge cases in regex bounds matching for the rules drift auditor where trailing words (e.g., limit, exposure limit) blocked parsed matches. Verified changes via Pytest. |

